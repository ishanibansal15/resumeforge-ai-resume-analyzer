# GitHub Setup

Suggested repository name:

`resumeforge-ai-resume-analyzer`

Suggested description:

`AI-inspired resume analysis dashboard for ATS readiness, skill gaps and job-role matching.`

Do not upload `node_modules`.

Quick Git workflow:

```bash
git init
git add .
git commit -m "Initial commit - ResumeForge AI"
git branch -M main
git remote add origin YOUR_REPOSITORY_URL
git push -u origin main
```
